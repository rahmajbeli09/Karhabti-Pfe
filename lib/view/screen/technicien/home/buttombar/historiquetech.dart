import 'package:flutter/material.dart';

class HistoriqueTech extends StatelessWidget {
  const HistoriqueTech({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("historique"),
    );
  }
}