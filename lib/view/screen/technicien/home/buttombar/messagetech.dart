import 'package:flutter/material.dart';

class MessageTech extends StatelessWidget {
  const MessageTech({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("messages"),
    );
  }
}