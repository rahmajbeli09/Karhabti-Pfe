import 'package:flutter/material.dart';

class NotificationTech extends StatelessWidget {
  const NotificationTech({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Notification"),
    );
  }
}